Simple C# console application where users can enter products until typing exit.
The program validates the product format (TEXT-NUMBER), ensures the number is between 200–500, and displays the products in a sorted list.

Concepts used: loops, arrays, string methods, input validation, and sorting.
